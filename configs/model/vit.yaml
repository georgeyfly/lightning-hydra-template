_target_: src.models.vit_module.ViTMNISTLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.1
  patience: 10

net:
  _target_: src.models.components.vit.ViT
  image_size: 28
  patch_size: 4
  num_classes: 10
  dim: 64
  depth: 3
  heads: 8
  mlp_dim: 128
  pool: cls
  channels: 1
  dim_head: 64
  dropout: 0.1

# compile model for faster training with pytorch 2.0
compile: false
